export const dynamicWidth = window.innerWidth * window.devicePixelRatio;

export const dynamicHeight = window.innerHeight * window.devicePixelRatio;

export const scaleRatio = window.devicePixelRatio / 3;